# No environment variables are required for the initialized application.
#
# When public client configuration is introduced, document its name here with
# an empty or explicitly non-secret example value. EXPO_PUBLIC_ values are
# embedded in the application bundle and must never contain private secrets,
# administrative credentials, or unrestricted server-side API keys.
